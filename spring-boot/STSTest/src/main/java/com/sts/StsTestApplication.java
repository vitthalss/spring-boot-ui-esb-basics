package com.sts;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Boot application configuration and main class.
 */
@SpringBootApplication
public class StsTestApplication {

    /**
     * Main Method to start Spring Boot application.
     *
     * @param args String Main Args
     */
    public static void main(final String[] args) {
        SpringApplication.run(StsTestApplication.class, args);
    }

    /**
     * Sample bootApplication method.
     */
    public void bootApplication() {
    }
}
